
class Commons{
  toUpperCase(String word){
    // var capital = '${word[0].toUpperCase()}${word.substring(1)}'; //uppercase only first letter
    var capital = '${word.toUpperCase()}';
    return capital;
  }
}
